import 'dotenv/config';

async function main() {
  console.log(
    '✅ Auth seed: Placeholder (seed data to be implemented in Checkpoint 7)'
  );
}

main().catch((e) => {
  console.error('❌ Seed failed:', e);
  process.exit(1);
});
