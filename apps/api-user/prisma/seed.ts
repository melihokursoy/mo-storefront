import 'dotenv/config';

async function main() {
  console.log(
    '✅ User seed: Placeholder (seed data to be implemented in Checkpoint 5)'
  );
}

main().catch((e) => {
  console.error('❌ Seed failed:', e);
  process.exit(1);
});
